<%= simple_form_for(pet) do |f| %>
  <%= f.input :name %>
  <%= f.input :species %>
  <%= f.input :description %>
  <%= f.input :price %>
  <%= f.input :address, input_html: { id: 'autocomplete_input'}%>
  <%= f.input :photo, as: :file %>
  <%= f.submit %>
<% end %>
