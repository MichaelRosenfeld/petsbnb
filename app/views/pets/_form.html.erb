<%= simple_form_for(pet) do |f| %>
  <%= f.input :name %>
  <%= f.input :species, collection: ["Dog", "Cat", "Bird", "Hamster"] %>
  <%= f.input :description %>
  <%= f.input :price %>
  <%= f.input :address %>
  <%= f.input :photo, as: :file %>
  <%= f.submit %>
<% end %>
